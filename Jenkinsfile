pipeline {
    stages {
        stage {
            // Main Branch
            try {
                // Main Branch Testing
            } catch (Exception E) {
                // Echo 'error'
            }
            // Publish HTML Report
        }
        stage {
            // Non-Main Branches
            try {
                // Non-Main Branch Testing
            } catch (Exception E) {
                // Echo 'error'
            }
            // Publish HTML Report
        }
    }
    post {
        success {
            // Echo 'pass'
        }
        failure {
            // Echo 'fail'
        }
    }
}
